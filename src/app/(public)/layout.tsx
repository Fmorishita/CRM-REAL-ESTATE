// Public marketing surfaces (landing pages). The root layout already provides
// theme + toast providers; this group intentionally has no app shell.
export default function PublicLayout({ children }: { children: React.ReactNode }) {
  return children;
}
